package ratpack.site

class RatpackVersions {

  final String current
  final String snapshot

  RatpackVersions(String current, String snapshot) {
    this.current = current
    this.snapshot = snapshot
  }
}
