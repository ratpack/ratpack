apply plugin: 'groovy'

dependencies {
  groovy localGroovy()
  compile gradleApi()
  testCompile 'org.spockframework:spock-core:0.7-groovy-1.8'
}

test {
  dependsOn ":ratpack-core:install", ":ratpack-groovy:install"
}